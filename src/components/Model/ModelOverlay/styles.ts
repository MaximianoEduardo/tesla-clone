import styled from 'styled-components';

export const Container = styled.div`
    position: sticky;
    height: 100vh;
    margin-top: -100vh;
    top: 0;
    display: flex;
    flex-direction: column;
    align-items: center
`;
